import 'package:flutter/material.dart';

class ModiStudioDekstop extends StatelessWidget {
  const ModiStudioDekstop({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.deepPurple[100],
      child: Center(
        child: Text('msd'),
      ),
    );
  }
}
