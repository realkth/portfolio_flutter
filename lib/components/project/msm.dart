import 'package:flutter/material.dart';

class ModiStudioMobile extends StatelessWidget {
  const ModiStudioMobile({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.amber[100],
      child: Center(
        child: Text('msm'),
      ),
    );
  }
}
