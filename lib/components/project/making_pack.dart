import 'package:flutter/material.dart';

class MakingPack extends StatelessWidget {
  const MakingPack({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.brown[100],
      child: Center(
        child: Text('makingPack'),
      ),
    );
  }
}
